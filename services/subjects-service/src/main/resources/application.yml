spring:
  application:
    name: subjects-service
  mongodb:
    host: localhost
    port: 27017
    database: subjects_service

server:
  port: 8004

security:
  jwt:
    secret: juangpt1234juangpt1234juangpt1234
  hmac:
    secret: ${HMAC_SECRET:change-this-hmac-secret}

integration:
  internal:
    service-id: subjects-service
  careers:
    service-name: carreras-service
    exists-path: ${CAREER_SERVICE_EXISTS_PATH:/api/carreras/internal/{careerId}/exists}
    exists-sign-path: ${CAREER_SERVICE_EXISTS_SIGN_PATH:/internal/{careerId}/exists}
